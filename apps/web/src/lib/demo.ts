/**
 * @deprecated Import directly from `@/lib/types`, `@/lib/seed`, or
 * `@/lib/dashboard` instead of this barrel.
 *
 * This shim exists only to keep existing `@/lib/demo` import paths working
 * during the migration. It will be removed once all consumers are updated.
 */

export * from "./types.ts";
export * from "./seed.ts";
export * from "./dashboard.ts";
